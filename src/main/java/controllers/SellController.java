package controllers;

import models.Result;
import models.data.Repository;

public class SellController extends Controller {
    public SellController(Repository repo) {
        super(repo);
    }

    @Override
    public Result handleCommand(String commandLine) {

    }
}
